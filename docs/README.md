Save a screenshot in this folder as `screenshot.png`, then uncomment the
image line near the top of the main [README](../README.md) so it shows up.
It is commented out rather than left in place because a reference to a
missing file renders as a broken image.

A good shot: the widget floating over a normal desktop background, so the
frameless transparent window and the mascot sitting above the card are both
obvious. A darker wallpaper shows it off best.

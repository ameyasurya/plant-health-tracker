Drop `screenshot.png` in this folder and it will appear at the top of the
main README automatically (it is already referenced there).

Good shot to take: the widget floating over a normal desktop background,
so the frameless transparent window and the mascot reading above the card
are both obvious. Dark background shows it off best.
